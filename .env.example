# Mocks are on by default. Set to "false" only to point at a real backend.
VITE_ENABLE_MSW=true

# REST base URL. Kept relative so MSW intercepts in dev, preview and the deploy.
VITE_API_BASE_URL=/api

# Socket.IO endpoint served by the MSW socket.io binding.
VITE_SOCKET_URL=/
